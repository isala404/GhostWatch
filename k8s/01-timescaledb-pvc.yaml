apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: timescaledb-pvc
  namespace: monitoring
spec:
  accessModes:
    - ReadWriteOnce # Suitable for a single TimescaleDB pod
  resources:
    requests:
      storage: 5Gi # Adjust size as needed for your demo